//! Dashboard command helpers.
//!
//! The standalone dashboard is started directly via `dashboard::start()` in
//! `dispatch/network/mod.rs`. This module is kept as an anchor for the
//! `cli::dashboard_cmd` mod declaration; add shared helpers here if needed.
