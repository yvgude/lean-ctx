---
import BaseLayout from '../layouts/BaseLayout.astro';
import TerminalChrome from '../components/TerminalChrome.astro';
import { getLocaleFromUrl, getLocalizedPath, useTranslations } from '../i18n/utils';
import { getMcpManifest } from '../lib/mcpManifest';
import '../styles/index.css';

const locale = getLocaleFromUrl(Astro.url);
const t = useTranslations(locale);
const lp = (path: string) => getLocalizedPath(path, locale);
const manifest = getMcpManifest();

const toolNames = manifest.tools.granular.map((x) => x.name);
const pickTool = (preferred: string, fallbackIndex: number) =>
  (toolNames.includes(preferred) ? preferred : toolNames[fallbackIndex] ?? toolNames[0]);

const tRead = pickTool('ctx_read', 0);
const tSearch = pickTool('ctx_search', 1);
const tShell = pickTool('ctx_shell', 2);

const faqJsonLd = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: [1,2,3,4,5].map(i => ({
    '@type': 'Question',
    name: t(`index.faq${i}Q`),
    acceptedAnswer: { '@type': 'Answer', text: t(`index.faq${i}A`) },
  })),
});
---

<BaseLayout title={t('index.title')} description={t('index.metaDesc')} extraJsonLd={faqJsonLd}>

<!-- ═══════════════════════════════════════════════════════
     1. HERO
     ═══════════════════════════════════════════════════════ -->
<section class="section-hero hero-split" data-hero>
  <div class="content-grid-wide">
    <div class="split split--visual-wide">
      <div class="hero-copy">
        <div class="animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
          <span class="kicker-pill">{t('index.heroLabel')}</span>
        </div>
        <h1 class="font-display text-text-bright animate-entrance hero-headline">
          {t('index.heroHeadline')}
        </h1>

        <p class="fluid-body text-muted animate-entrance stagger-1" style="margin-top: clamp(1.25rem, 1rem + 1vw, 2rem); max-width: 46rem;">
          {t('index.heroSubline')}
        </p>

        <div class="flex flex-wrap items-center animate-entrance stagger-2" style="margin-top: clamp(1.5rem, 1rem + 1.25vw, 2.5rem); gap: clamp(0.5rem, 0.375rem + 0.5vw, 1rem);">
          <a href={lp('/docs/getting-started')} class="btn-primary inline-flex items-center gap-2" style="padding: clamp(0.625rem, 0.5rem + 0.3vw, 0.875rem) clamp(0.875rem, 0.75rem + 0.4vw, 1.25rem); font-size: clamp(0.8125rem, 0.78rem + 0.12vw, 0.9375rem);">
            {t('index.heroCta')}
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
          </a>
          <a href={lp('/context-os/benchmark')} class="btn-outline inline-flex items-center gap-2" style="padding: clamp(0.625rem, 0.5rem + 0.3vw, 0.875rem) clamp(0.875rem, 0.75rem + 0.4vw, 1.25rem); font-size: clamp(0.8125rem, 0.78rem + 0.12vw, 0.9375rem);">
            {t('index.heroCtaSecondary')}
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
          </a>
          <a href="https://github.com/yvgude/lean-ctx" target="_blank" rel="noopener" class="btn-outline inline-flex items-center gap-2" style="padding: clamp(0.625rem, 0.5rem + 0.3vw, 0.875rem) clamp(0.875rem, 0.75rem + 0.4vw, 1.25rem); font-size: clamp(0.8125rem, 0.78rem + 0.12vw, 0.9375rem);">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
            {t('index.starOnGithub')}
          </a>
        </div>

        <div class="hero-proof animate-entrance stagger-2" style="margin-top: clamp(2rem, 1.5rem + 1.5vw, 3rem);">
          <div class="hero-proof-item">
            <span class="hero-proof-number font-display" id="install-count">-</span>
            <span class="hero-proof-label">{t('index.heroProofInstalls')}</span>
          </div>
          <div class="hero-proof-divider"></div>
          <div class="hero-proof-item">
            <span class="hero-proof-number font-display" id="github-stars-hero">-</span>
            <span class="hero-proof-label">{t('index.heroProofStars')}</span>
          </div>
          <div class="hero-proof-divider"></div>
          <div class="hero-proof-item">
            <span class="hero-proof-number font-display">{manifest.counts.granular}</span>
            <span class="hero-proof-label">{t('index.heroProofTools')}</span>
          </div>
        </div>
      </div>

      <div class="hero-terminal-wrap animate-entrance stagger-2">
        <div class="hero-terminal" data-hero-terminal>
          <div class="terminal-header flex items-center gap-2" style="padding: clamp(0.5rem, 0.375rem + 0.3vw, 0.75rem) clamp(0.75rem, 0.625rem + 0.4vw, 1.25rem);">
            <span class="font-mono text-[10px] text-[#ff5f57]">[x]</span>
            <span class="font-mono text-[10px] text-[#febc2e]">[-]</span>
            <span class="font-mono text-[10px] text-[#28c840]">[o]</span>
            <span class="ml-3 text-xs text-muted font-mono">lean-ctx session</span>
          </div>
          <div class="terminal-body font-mono" style="padding: clamp(0.75rem, 0.625rem + 0.4vw, 1.25rem); font-size: clamp(0.6rem, 0.55rem + 0.12vw, 0.8125rem); line-height: 1.75;">
            <div class="hero-term-line" data-term-line="0">
              <span class="text-muted">$</span> <span class="text-accent">lean-ctx</span> <span class="text-text-bright">read src/lib/auth.ts -m map</span>
            </div>
            <div class="hero-term-line hero-term-result" data-term-line="1">
              <span class="text-muted">→</span> <span class="text-text">exports: authenticate(), validateToken(), refreshSession()</span>
            </div>
            <div class="hero-term-line hero-term-result" data-term-line="2">
              <span class="text-muted">→</span> <span class="text-text">4,200 → <span class="text-accent font-semibold">180 tokens</span> (96% saved)</span>
            </div>
            <div class="hero-term-line hero-term-result" data-term-line="3">
              <span class="text-muted">→</span> <span class="text-text">cached: <span class="text-accent">13 tokens</span> on re-read</span>
            </div>
            <div class="hero-term-spacer" data-term-line="4"></div>
            <div class="hero-term-line" data-term-line="5">
              <span class="text-muted">$</span> <span class="text-accent">lean-ctx</span> <span class="text-text-bright">grep "authenticate" src/</span>
            </div>
            <div class="hero-term-line hero-term-result" data-term-line="6">
              <span class="text-muted">→</span> <span class="text-text">3 matches in <span class="text-accent">0.8ms</span></span>
            </div>
            <div class="hero-term-line hero-term-result" data-term-line="7">
              <span class="text-muted">→</span> <span class="text-text">auth.ts:14, middleware.ts:8, routes.ts:23</span>
            </div>
            <div class="hero-term-spacer" data-term-line="8"></div>
            <div class="hero-term-line" data-term-line="9">
              <span class="text-muted">$</span> <span class="text-accent">lean-ctx</span> <span class="text-text-bright">-c "cargo build --release"</span>
            </div>
            <div class="hero-term-line hero-term-result" data-term-line="10">
              <span class="text-muted">→</span> <span class="text-text">Compiling lean-ctx v3.6.0 in <span class="text-accent">3.6s</span></span>
            </div>
            <div class="hero-term-line hero-term-result" data-term-line="11">
              <span class="text-muted">→</span> <span class="text-text">compressed: <span class="text-accent">847 → 42 tokens</span></span>
            </div>
            <div class="hero-term-cursor" data-term-line="12"></div>
          </div>
        </div>
        <div class="hero-install animate-entrance stagger-4">
          <code class="hero-install-code" id="hero-install-cmd">curl -fsSL https://leanctx.com/install.sh | sh</code>
          <button class="hero-copy-btn" type="button" data-copy="curl -fsSL https://leanctx.com/install.sh | sh" aria-label="Copy install command">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/></svg>
          </button>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ═══════════════════════════════════════════════════════
     2. PROBLEM — Before / After
     ═══════════════════════════════════════════════════════ -->
<section class="section-y">
  <div class="content-grid-wide">
    <div class="split split--visual-wide">
      <div>
        <div class="animate-entrance" style="margin-bottom: clamp(1.25rem, 1rem + 0.75vw, 2rem);">
          <span class="kicker-pill">{t('index.problemLabel')}</span>
        </div>
        <h2 class="font-display fluid-h1 text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.5rem, 1rem + 1vw, 2.5rem); max-width: 54rem;">
          {t('index.problemTitle')}
        </h2>
        <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(1.25rem, 1rem + 0.75vw, 2rem); max-width: 52rem;">
          {t('index.problemDesc')}
        </p>
        <p class="fluid-body text-text-bright font-medium animate-entrance stagger-2" style="margin: 0; max-width: 52rem; font-style: italic;">
          {t('index.problemQuote')}
        </p>
      </div>

      <div class="animate-entrance stagger-3">
        <div class="compare-grid is-stacked">
      <div class="compare-card">
        <div class="compare-header compare-header--bad">
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636"/></svg>
          {t('index.withoutLeanCtx')}
        </div>
        <div class="compare-body font-mono" style="font-size: clamp(0.75rem, 0.7rem + 0.12vw, 0.875rem);">
          <div class="compare-line"><span class="text-muted">{t('index.without1')}</span></div>
          <div class="compare-line"><span class="text-danger">{t('index.without2')}</span></div>
          <div class="compare-line"><span class="text-muted">{t('index.without3')}</span></div>
          <div class="compare-line"><span class="text-danger">{t('index.without4')}</span></div>
        </div>
      </div>
      <div class="compare-card">
        <div class="compare-header compare-header--good">
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          {t('index.withLeanCtx')}
        </div>
        <div class="compare-body font-mono" style="font-size: clamp(0.75rem, 0.7rem + 0.12vw, 0.875rem);">
          <div class="compare-line"><span class="text-muted">{t('index.with1')}</span></div>
          <div class="compare-line"><span class="text-accent">{t('index.with2')}</span></div>
          <div class="compare-line"><span class="text-muted">{t('index.with3')}</span></div>
          <div class="compare-line"><span class="text-accent">{t('index.with4')}</span></div>
        </div>
      </div>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ═══════════════════════════════════════════════════════
     3. HOW IT WORKS — Four capabilities
     ═══════════════════════════════════════════════════════ -->
<section class="section-y">
  <div class="content-grid">
    <div class="animate-entrance" style="margin-bottom: clamp(1.25rem, 1rem + 0.75vw, 2rem);">
      <span class="kicker-pill">{t('index.howItWorksLabel')}</span>
    </div>
    <h2 class="font-display fluid-h1 text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.5rem, 1rem + 1vw, 2.5rem); max-width: 48rem;">
      {t('index.howItWorksTitle')} <span class="italic-accent">{t('index.howItWorksTitleHighlight')}</span>
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(4rem, 3rem + 4vw, 7rem); max-width: 48rem;">
      {t('index.howItWorksDesc')}
    </p>

    <div class="grid md:grid-cols-2 lg:grid-cols-4 animate-entrance stagger-3" style="gap: clamp(1.5rem, 1rem + 1.5vw, 2.5rem);">
      <a href={lp('/docs/concepts/read-modes')} class="what-card group" style="text-decoration: none;">
        <div class="what-icon" style="color: var(--color-accent);">
          <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M3.75 13.5l10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75z"/></svg>
        </div>
        <h3 class="font-display fluid-h3 font-semibold text-text-bright" style="margin: 1.25rem 0 0.625rem;">{t('index.howItWorks1Title')}</h3>
        <p class="fluid-sm text-muted leading-relaxed">{t('index.howItWorks1Desc')}</p>
      </a>

      <a href={lp('/docs/concepts/caching')} class="what-card group" style="text-decoration: none;">
        <div class="what-icon" style="color: var(--color-accent-2);">
          <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M20.25 6.375c0 2.278-3.694 4.125-8.25 4.125S3.75 8.653 3.75 6.375m16.5 0c0-2.278-3.694-4.125-8.25-4.125S3.75 4.097 3.75 6.375m16.5 0v11.25c0 2.278-3.694 4.125-8.25 4.125s-8.25-1.847-8.25-4.125V6.375m16.5 0v3.75m-16.5-3.75v3.75m16.5 0v3.75C20.25 16.153 16.556 18 12 18s-8.25-1.847-8.25-4.125v-3.75m16.5 0c0 2.278-3.694 4.125-8.25 4.125s-8.25-1.847-8.25-4.125"/></svg>
        </div>
        <h3 class="font-display fluid-h3 font-semibold text-text-bright" style="margin: 1.25rem 0 0.625rem;">{t('index.howItWorks2Title')}</h3>
        <p class="fluid-sm text-muted leading-relaxed">{t('index.howItWorks2Desc')}</p>
      </a>

      <a href={lp('/docs/configuration')} class="what-card group" style="text-decoration: none;">
        <div class="what-icon" style="color: var(--color-accent-3);">
          <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        </div>
        <h3 class="font-display fluid-h3 font-semibold text-text-bright" style="margin: 1.25rem 0 0.625rem;">{t('index.howItWorks3Title')}</h3>
        <p class="fluid-sm text-muted leading-relaxed">{t('index.howItWorks3Desc')}</p>
      </a>

      <a href={lp('/docs/security')} class="what-card group" style="text-decoration: none;">
        <div class="what-icon" style="color: var(--color-accent);">
          <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12c0 1.268-.63 2.39-1.593 3.068a3.745 3.745 0 01-1.043 3.296 3.745 3.745 0 01-3.296 1.043A3.745 3.745 0 0112 21c-1.268 0-2.39-.63-3.068-1.593a3.746 3.746 0 01-3.296-1.043 3.745 3.745 0 01-1.043-3.296A3.745 3.745 0 013 12c0-1.268.63-2.39 1.593-3.068a3.745 3.745 0 011.043-3.296 3.746 3.746 0 013.296-1.043A3.746 3.746 0 0112 3c1.268 0 2.39.63 3.068 1.593a3.746 3.746 0 013.296 1.043 3.745 3.745 0 011.043 3.296A3.745 3.745 0 0121 12z"/></svg>
        </div>
        <h3 class="font-display fluid-h3 font-semibold text-text-bright" style="margin: 1.25rem 0 0.625rem;">{t('index.howItWorks4Title')}</h3>
        <p class="fluid-sm text-muted leading-relaxed">{t('index.howItWorks4Desc')}</p>
      </a>
    </div>

    <div class="grid md:grid-cols-3 animate-entrance stagger-4" style="margin-top: clamp(2rem, 1.5rem + 1vw, 3rem); gap: clamp(0.75rem, 0.5rem + 0.5vw, 1rem);">
      <div class="trust-badge">
        <svg class="w-4 h-4 text-accent shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/></svg>
        <span class="fluid-sm text-text-bright font-medium">OWASP Agentic Top 10 aligned (8/10)</span>
      </div>
      <div class="trust-badge">
        <svg class="w-4 h-4 text-accent shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        <span class="fluid-sm text-text-bright font-medium">OS-level sandboxing (Seatbelt + Landlock)</span>
      </div>
      <div class="trust-badge">
        <svg class="w-4 h-4 text-accent shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 10-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 002.25-2.25v-6.75a2.25 2.25 0 00-2.25-2.25H6.75a2.25 2.25 0 00-2.25 2.25v6.75a2.25 2.25 0 002.25 2.25z"/></svg>
        <span class="fluid-sm text-text-bright font-medium">SHA-256 chained audit trail</span>
      </div>
    </div>

    <div class="animate-entrance stagger-5" style="margin-top: clamp(2rem, 1.5rem + 1vw, 3rem); display: flex; flex-wrap: wrap; gap: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      <a href={lp('/context-os')} class="fluid-sm text-muted hover:text-text-bright transition-colors inline-flex items-center gap-1.5">
        {t('index.heroCtaSecondary')}
        <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
      </a>
      <a href={lp('/docs/concepts/shell-patterns')} class="fluid-sm text-muted hover:text-text-bright transition-colors inline-flex items-center gap-1.5">
        {t('index.shellPatternsLink')}
        <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
      </a>
    </div>
  </div>
</section>


<!-- ═══════════════════════════════════════════════════════
     4. LIVE DEMO — Terminal preview
     ═══════════════════════════════════════════════════════ -->
<section class="section-y" data-preview style="--pp: 0;">
  <div class="content-grid-wide">
    <div class="split split--visual-wide">
      <div>
        <div class="animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
          <span class="kicker-pill">{t('index.previewLabel')}</span>
        </div>
        <h2 class="font-display fluid-h1 text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.5rem, 1rem + 1vw, 2.5rem); max-width: 54rem;">
          {t('index.previewTitle')}
        </h2>
        <p class="text-muted fluid-body animate-entrance stagger-2" style="margin: 0; max-width: 48rem;">
          {t('index.previewDesc')}
        </p>
      </div>

      <TerminalChrome
        title={`${tRead} - map`}
        chromeClass="animate-entrance stagger-3"
        bodyStyle="font-size: clamp(0.72rem, 0.68rem + 0.1vw, 0.8125rem); line-height: 1.75;"
      >
        <div>
          <span class="text-accent">{t('index.demoAiCallsLabel')}</span>{' '}
          <span class="text-text-bright">{`${tRead}({ path: "src/lib/mcpManifest.ts", mode: "map" })`}</span>
        </div>
        <div class="term-divider"></div>
        <div><span class="text-muted">schema_version</span> <span class="text-text-bright">{manifest.schema_version}</span></div>
        <div><span class="text-muted">tools.granular</span> <span class="text-accent font-semibold">{manifest.counts.granular}</span></div>
        <div><span class="text-muted">tools.unified</span> <span class="text-text-bright">{manifest.counts.unified}</span></div>
        <div><span class="text-muted">read_modes</span> <span class="text-text-bright">{manifest.read_modes.count}</span></div>
        <div class="term-divider"></div>
        <div class="text-muted">modes: <span class="text-text">{manifest.read_modes.modes.slice(0, 6).join(', ')}</span></div>
      </TerminalChrome>
    </div>
  </div>
</section>


<!-- ═══════════════════════════════════════════════════════
     5. NUMBERS STRIP
     ═══════════════════════════════════════════════════════ -->
<section class="section-y-sm">
  <div class="content-grid">
    <div class="stats-panels animate-entrance">
      <div class="stat-panel">
        <span class="stat-number text-accent">60–99%</span>
        <span class="stat-desc">{t('index.statsReduction')}</span>
        <a href={lp('/context-os/benchmark')} class="stat-methodology-link">See methodology</a>
      </div>
      <div class="stat-panel">
        <span class="stat-number">{manifest.counts.granular}</span>
        <span class="stat-desc">{t('index.statsTools')}</span>
      </div>
      <div class="stat-panel">
        <span class="stat-number">{manifest.read_modes.count}</span>
        <span class="stat-desc">{t('index.statsReadModes')}</span>
      </div>
      <div class="stat-panel">
        <span class="stat-number text-accent">0</span>
        <span class="stat-desc">{t('index.statsConfig')}</span>
      </div>
    </div>
  </div>
</section>


<!-- ═══════════════════════════════════════════════════════
     6. COMPATIBILITY
     ═══════════════════════════════════════════════════════ -->
<section class="section-y">
  <div class="content-grid">
    <div class="animate-entrance" style="margin-bottom: clamp(1.25rem, 1rem + 0.75vw, 2rem);">
      <span class="kicker-pill">{t('index.compatLabel')}</span>
    </div>
    <h2 class="font-display fluid-h1 text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.5rem, 1rem + 1vw, 2.5rem);">
      {t('index.compatTitle')}
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(4rem, 3rem + 4vw, 7rem); max-width: 42rem;">
      {t('index.compatDesc')}
    </p>

    <div class="compat-grid animate-entrance stagger-3">
      {[
        'Aider', 'Amazon Q', 'Amp', 'Antigravity', 'AWS Kiro', 'Claude Code',
        'Cline', 'Continue', 'Cursor', 'Emacs', 'Gemini CLI', 'GitHub Copilot',
        'JetBrains', 'Neovim', 'OpenAI Codex', 'OpenCode', 'Pi', 'Qwen Code',
        'Roo Code', 'Sublime Text', 'Trae', 'Verdent', 'Windsurf', 'Zed',
      ].map(name => (
        <span class="compat-item">{name}</span>
      ))}
    </div>

    <div class="animate-entrance stagger-4" style="margin-top: clamp(2.5rem, 2rem + 1.5vw, 4rem);">
      <a href={lp('/compatibility')} class="fluid-sm text-muted hover:text-text-bright transition-colors inline-flex items-center gap-1.5">
        {t('index.compatMatrix')}
        <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
      </a>
    </div>
  </div>
</section>


<!-- ═══════════════════════════════════════════════════════
     7. FAQ (SEO + Rich Results)
     ═══════════════════════════════════════════════════════ -->
<section class="section-y" id="faq">
  <div class="content-grid">
    <div class="animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      <span class="kicker-pill">{t('index.faqKicker')}</span>
    </div>
    <h2 class="font-display fluid-h1 text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(3rem, 2rem + 2vw, 5rem); max-width: 48rem;">
      {t('index.faqTitle')}
    </h2>
    <div class="faq-grid" style="display: grid; gap: clamp(1.5rem, 1rem + 1vw, 2.5rem); max-width: 48rem;">
      {[1,2,3,4,5].map(i => (
        <details class="faq-item" style="border-bottom: 1px solid var(--border); padding-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
          <summary class="fluid-body font-semibold text-text-bright" style="cursor: pointer; list-style: none; display: flex; justify-content: space-between; align-items: center;">
            {t(`index.faq${i}Q`)}
            <svg class="faq-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
          </summary>
          <p class="text-muted fluid-sm" style="margin-top: clamp(0.75rem, 0.5rem + 0.5vw, 1.25rem);">
            {t(`index.faq${i}A`)}
          </p>
        </details>
      ))}
    </div>
  </div>
</section>


<!-- ═══════════════════════════════════════════════════════
     8. CTA
     ═══════════════════════════════════════════════════════ -->
<section class="section-y">
  <div class="content-grid">
    <div class="animate-entrance">
      <h2 class="font-display fluid-display text-text-bright" style="margin-bottom: clamp(1.5rem, 1rem + 1.5vw, 3rem); max-width: 48rem;">
        {t('index.ctaTitle')}
      </h2>
      <p class="text-muted fluid-body" style="margin-bottom: clamp(3rem, 2rem + 2vw, 5rem); max-width: 36rem;">
        {t('index.ctaDesc')}
      </p>

      <div class="flex flex-wrap items-center" style="gap: clamp(0.875rem, 0.5rem + 0.75vw, 1.5rem);">
        <a href={lp('/docs/getting-started')} class="btn-primary inline-flex items-center gap-2.5" style="padding: clamp(0.875rem, 0.7rem + 0.4vw, 1.125rem) clamp(2rem, 1.5rem + 1vw, 3rem); font-size: clamp(0.875rem, 0.84rem + 0.15vw, 1rem);">
          {t('nav.getStarted')}
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
        </a>
        <a href="https://github.com/yvgude/lean-ctx" target="_blank" rel="noopener" class="btn-outline inline-flex items-center gap-2.5" style="padding: clamp(0.875rem, 0.7rem + 0.4vw, 1.125rem) clamp(2rem, 1.5rem + 1vw, 3rem); font-size: clamp(0.875rem, 0.84rem + 0.15vw, 1rem);">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
          {t('index.starOnGithub')}
        </a>
      </div>

    </div>
  </div>
</section>

<script is:inline>
(function() {
  var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  var installEl = document.getElementById('install-count');
  var starsEl = document.getElementById('github-stars-hero');

  function countUp(el, target, duration) {
    var start = 0;
    var startTime = null;
    function step(ts) {
      if (!startTime) startTime = ts;
      var progress = Math.min((ts - startTime) / duration, 1);
      var ease = 1 - Math.pow(1 - progress, 3);
      var current = Math.floor(ease * target);
      el.textContent = current.toLocaleString(undefined);
      if (progress < 1) requestAnimationFrame(step);
    }
    requestAnimationFrame(step);
  }

  fetch('/api/stats')
    .then(function(r) { return r.json(); })
    .then(function(d) {
      if (d.installs > 0 && installEl) countUp(installEl, d.installs, 1200);
      if (d.stars > 0 && starsEl) countUp(starsEl, d.stars, 1000);
    })
    .catch(function() {});

  var copyBtns = document.querySelectorAll('[data-copy]');
  copyBtns.forEach(function(btn) {
    btn.addEventListener('click', function() {
      var text = btn.getAttribute('data-copy');
      if (navigator.clipboard && text) {
        navigator.clipboard.writeText(text).then(function() {
          btn.classList.add('copied');
          setTimeout(function() { btn.classList.remove('copied'); }, 1500);
        });
      }
    });
  });

  if (reduceMotion) return;
})();
</script>

<style>
  .hero-headline {
    font-size: clamp(2.25rem, 1.5rem + 3.5vw, 4.5rem);
    line-height: 1.15;
    letter-spacing: -0.04em;
    text-wrap: balance;
    max-width: 18ch;
  }
  .stat-methodology-link {
    display: block;
    margin-top: 0.5rem;
    font-size: clamp(0.6875rem, 0.65rem + 0.1vw, 0.8125rem);
    color: var(--color-accent);
    text-decoration: none;
    opacity: 0.7;
    transition: opacity 0.2s ease;
  }
  .stat-methodology-link:hover {
    opacity: 1;
  }
  .trust-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: clamp(0.625rem, 0.5rem + 0.25vw, 0.875rem) clamp(0.75rem, 0.625rem + 0.3vw, 1rem);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    background: color-mix(in srgb, var(--color-accent) 4%, var(--color-surface));
  }
</style>

</BaseLayout>
