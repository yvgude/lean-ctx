---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageHero from '../components/PageHero.astro';

import { getLocaleFromUrl, getLocalizedPath, useTranslations } from '../i18n/utils';

const locale = getLocaleFromUrl(Astro.url);
const t = useTranslations(locale);
const lp = (path: string) => getLocalizedPath(path, locale);
---

<BaseLayout
  title={t('protocolsCep.title')}
  description={t('protocolsCep.description')}
  breadcrumbs={[
    { name: t('common.home'), url: lp('/') },
    { name: t('protocolsIndex.heroLabel'), url: lp('/protocols') },
    { name: 'CEP', url: lp('/protocols/cep') },
  ]}
>

<!-- ═══════ HERO ═══════ -->
<PageHero
  label={t('protocolsCep.heroLabel')}
  title={t('protocolsCep.heroTitle')}
  description={t('protocolsCep.heroDesc')}
  highlightStyle="italic"
>
  <Fragment slot="actions">
    <a href={lp('/docs/getting-started')} class="btn-primary inline-flex items-center gap-2 rounded-lg" style="padding: clamp(0.7rem, 0.55rem + 0.3vw, 0.9rem) clamp(1.5rem, 1rem + 0.75vw, 2.25rem); font-size: clamp(0.8125rem, 0.78rem + 0.15vw, 0.9375rem);">
      {t('protocolsCep.getStarted')}
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
    </a>
    <a href={lp('/protocols')} class="btn-outline inline-flex items-center gap-2 rounded-lg" style="padding: clamp(0.7rem, 0.55rem + 0.3vw, 0.9rem) clamp(1.5rem, 1rem + 0.75vw, 2.25rem); font-size: clamp(0.8125rem, 0.78rem + 0.15vw, 0.9375rem);">
      {t('protocolsCep.allProtocols')}
    </a>
  </Fragment>
</PageHero>

<div class="section-divider content-grid"></div>

<!-- ═══════ WHAT CEP DOES ═══════ -->
<section class="section-y">
  <div class="content-grid">
    <p class="fluid-label text-muted animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      {t('protocolsCep.whatLabel')}
    </p>
    <h2 class="font-display fluid-h2 font-bold text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.25rem, 1rem + 0.5vw, 2rem);">
      {t('protocolsCep.whatTitle')}
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(3rem, 2rem + 3vw, 6rem); max-width: 48rem;">
      {t('protocolsCep.whatDesc')}
    </p>

    <div class="grid sm:grid-cols-2 lg:grid-cols-4 animate-entrance stagger-3" style="gap: clamp(0.75rem, 0.5rem + 0.5vw, 1rem);">
      <div class="cep-how-card">
        <span class="cep-how-number font-display text-accent">01</span>
        <p class="fluid-sm text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.step1Title')}</p>
        <p class="fluid-sm text-muted">{t('protocolsCep.step1Desc')}</p>
      </div>
      <div class="cep-how-card">
        <span class="cep-how-number font-display text-accent">02</span>
        <p class="fluid-sm text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.step2Title')}</p>
        <p class="fluid-sm text-muted">{t('protocolsCep.step2Desc')}</p>
      </div>
      <div class="cep-how-card">
        <span class="cep-how-number font-display text-accent">03</span>
        <p class="fluid-sm text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.step3Title')}</p>
        <p class="fluid-sm text-muted">{t('protocolsCep.step3Desc')}</p>
      </div>
      <div class="cep-how-card">
        <span class="cep-how-number font-display text-accent">04</span>
        <p class="fluid-sm text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.step4Title')}</p>
        <p class="fluid-sm text-muted">{t('protocolsCep.step4Desc')}</p>
      </div>
    </div>
  </div>
</section>

<div class="section-divider content-grid"></div>

<!-- ═══════ THE FOUR DIMENSIONS ═══════ -->
<section class="section-y">
  <div class="content-grid">
    <p class="fluid-label text-muted animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      {t('protocolsCep.scoringLabel')}
    </p>
    <h2 class="font-display fluid-h2 font-bold text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.25rem, 1rem + 0.5vw, 2rem);">
      {t('protocolsCep.scoringTitle')}
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(3rem, 2rem + 3vw, 6rem); max-width: 48rem;">
      {t('protocolsCep.scoringDesc')}
    </p>

    <div class="grid sm:grid-cols-2 animate-entrance stagger-3" style="gap: clamp(0.75rem, 0.5rem + 0.5vw, 1rem);">
      <div class="cep-dim-card">
        <div class="cep-dim-header">
          <span class="cep-dim-icon text-accent">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M20.25 6.375c0 2.278-3.694 4.125-8.25 4.125S3.75 8.653 3.75 6.375m16.5 0c0-2.278-3.694-4.125-8.25-4.125S3.75 4.097 3.75 6.375m16.5 0v11.25c0 2.278-3.694 4.125-8.25 4.125s-8.25-1.847-8.25-4.125V6.375m16.5 0v3.75m-16.5-3.75v3.75m16.5 0v3.75C20.25 16.153 16.556 18 12 18s-8.25-1.847-8.25-4.125v-3.75m16.5 0c0 2.278-3.694 4.125-8.25 4.125s-8.25-1.847-8.25-4.125"/></svg>
          </span>
          <h3 class="font-display fluid-h3 font-semibold text-text-bright">{t('protocolsCep.dimCacheTitle')}</h3>
        </div>
        <p class="fluid-sm text-muted" style="margin-top: 0.5rem;">
          {t('protocolsCep.dimCacheDesc')}
        </p>
        <p class="cep-dim-metric font-mono text-accent">{t('protocolsCep.dimCacheMetric')}</p>
      </div>

      <div class="cep-dim-card">
        <div class="cep-dim-header">
          <span class="cep-dim-icon text-accent-2">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6A2.25 2.25 0 016 3.75h2.25A2.25 2.25 0 0110.5 6v2.25a2.25 2.25 0 01-2.25 2.25H6a2.25 2.25 0 01-2.25-2.25V6zM3.75 15.75A2.25 2.25 0 016 13.5h2.25a2.25 2.25 0 012.25 2.25V18a2.25 2.25 0 01-2.25 2.25H6A2.25 2.25 0 013.75 18v-2.25zM13.5 6a2.25 2.25 0 012.25-2.25H18A2.25 2.25 0 0120.25 6v2.25A2.25 2.25 0 0118 10.5h-2.25a2.25 2.25 0 01-2.25-2.25V6zM13.5 15.75a2.25 2.25 0 012.25-2.25H18a2.25 2.25 0 012.25 2.25V18A2.25 2.25 0 0118 20.25h-2.25A2.25 2.25 0 0113.5 18v-2.25z"/></svg>
          </span>
          <h3 class="font-display fluid-h3 font-semibold text-text-bright">{t('protocolsCep.dimModesTitle')}</h3>
        </div>
        <p class="fluid-sm text-muted" style="margin-top: 0.5rem;" set:html={t('protocolsCep.dimModesDesc')} />
        <p class="cep-dim-metric font-mono text-accent-2">{t('protocolsCep.dimModesMetric')}</p>
      </div>

      <div class="cep-dim-card">
        <div class="cep-dim-header">
          <span class="cep-dim-icon text-accent-3">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M3 4.5h14.25M3 9h9.75M3 13.5h9.75m4.5-4.5v12m0 0l-3.75-3.75M17.25 21l3.75-3.75"/></svg>
          </span>
          <h3 class="font-display fluid-h3 font-semibold text-text-bright">{t('protocolsCep.dimCompressTitle')}</h3>
        </div>
        <p class="fluid-sm text-muted" style="margin-top: 0.5rem;">
          {t('protocolsCep.dimCompressDesc')}
        </p>
        <p class="cep-dim-metric font-mono text-accent-3">{t('protocolsCep.dimCompressMetric')}</p>
      </div>

      <div class="cep-dim-card">
        <div class="cep-dim-header">
          <span class="cep-dim-icon" style="color: var(--color-text-bright);">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M3.75 13.5l10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75z"/></svg>
          </span>
          <h3 class="font-display fluid-h3 font-semibold text-text-bright">{t('protocolsCep.dimActionTitle')}</h3>
        </div>
        <p class="fluid-sm text-muted" style="margin-top: 0.5rem;">
          {t('protocolsCep.dimActionDesc')}
        </p>
        <p class="cep-dim-metric font-mono text-text-bright">{t('protocolsCep.dimActionMetric')}</p>
      </div>
    </div>
  </div>
</section>

<div class="section-divider content-grid"></div>

<!-- ═══════ COMPLEXITY LEVELS ═══════ -->
<section class="section-y">
  <div class="content-grid">
    <p class="fluid-label text-muted animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      {t('protocolsCep.complexityLabel')}
    </p>
    <h2 class="font-display fluid-h2 font-bold text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.25rem, 1rem + 0.5vw, 2rem);">
      {t('protocolsCep.complexityTitle')}
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(3rem, 2rem + 3vw, 6rem); max-width: 48rem;">
      {t('protocolsCep.complexityDesc')}
    </p>

    <div class="grid md:grid-cols-3 animate-entrance stagger-3" style="gap: clamp(0.75rem, 0.5rem + 0.5vw, 1rem);">
      <div class="cep-level-card">
        <span class="cep-level-dot" style="background: var(--color-accent);"></span>
        <h3 class="font-display fluid-h3 font-semibold text-text-bright" style="margin: 0.75rem 0 0.375rem;">{t('protocolsCep.mechanicalTitle')}</h3>
        <p class="fluid-sm text-muted" style="margin-bottom: 1rem;">{t('protocolsCep.mechanicalDesc')}</p>
        <div class="cep-level-instruction font-mono">
          <span class="text-accent">ACT1</span> <span class="text-muted">&middot;</span> <span class="text-accent">DELTA</span> <span class="text-muted">&middot;</span> <span class="text-accent">1LINE</span>
        </div>
      </div>

      <div class="cep-level-card">
        <span class="cep-level-dot" style="background: var(--color-accent-2);"></span>
        <h3 class="font-display fluid-h3 font-semibold text-text-bright" style="margin: 0.75rem 0 0.375rem;">{t('protocolsCep.standardTitle')}</h3>
        <p class="fluid-sm text-muted" style="margin-bottom: 1rem;">{t('protocolsCep.standardDesc')}</p>
        <div class="cep-level-instruction font-mono">
          <span class="text-accent-2">BRIEF</span> <span class="text-muted">&middot;</span> <span class="text-accent-2">DELTA</span> <span class="text-muted">&middot;</span> <span class="text-accent-2">NOREPEAT</span> <span class="text-muted">&middot;</span> <span class="text-accent-2">STRUCT</span>
        </div>
      </div>

      <div class="cep-level-card">
        <span class="cep-level-dot" style="background: var(--color-accent-3);"></span>
        <h3 class="font-display fluid-h3 font-semibold text-text-bright" style="margin: 0.75rem 0 0.375rem;">{t('protocolsCep.architecturalTitle')}</h3>
        <p class="fluid-sm text-muted" style="margin-bottom: 1rem;">{t('protocolsCep.architecturalDesc')}</p>
        <div class="cep-level-instruction font-mono">
          <span class="text-accent-3">FULL</span> <span class="text-muted">&middot;</span> <span class="text-accent-3">QUALITY</span> <span class="text-muted">&middot;</span> <span class="text-accent-3">NOREPEAT</span> <span class="text-muted">&middot;</span> <span class="text-accent-3">STRUCT</span> <span class="text-muted">&middot;</span> <span class="text-accent-3">FREF</span>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="section-divider content-grid"></div>

<!-- ═══════ INSTRUCTION CODES ═══════ -->
<section class="section-y">
  <div class="content-grid">
    <p class="fluid-label text-muted animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      {t('protocolsCep.refLabel')}
    </p>
    <h2 class="font-display fluid-h2 font-bold text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.25rem, 1rem + 0.5vw, 2rem);">
      {t('protocolsCep.refTitle')}
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(3rem, 2rem + 3vw, 6rem); max-width: 48rem;">
      {t('protocolsCep.refDesc')}
    </p>

    <div class="cep-table-wrap animate-entrance stagger-3">
      <table class="cep-table">
        <thead>
          <tr>
            <th>{t('protocolsCep.thCode')}</th>
            <th>{t('protocolsCep.thDescription')}</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code class="cep-code text-accent">ACT1</code></td>
            <td>{t('protocolsCep.codeACT1')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">BRIEF</code></td>
            <td>{t('protocolsCep.codeBRIEF')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">FULL</code></td>
            <td>{t('protocolsCep.codeFULL')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">DELTA</code></td>
            <td>{t('protocolsCep.codeDELTA')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">NOREPEAT</code></td>
            <td>{t('protocolsCep.codeNOREPEAT')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">STRUCT</code></td>
            <td>{t('protocolsCep.codeSTRUCT')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">1LINE</code></td>
            <td>{t('protocolsCep.code1LINE')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">NODOC</code></td>
            <td>{t('protocolsCep.codeNODOC')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">ACTFIRST</code></td>
            <td>{t('protocolsCep.codeACTFIRST')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">QUALITY</code></td>
            <td>{t('protocolsCep.codeQUALITY')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">NOMOCK</code></td>
            <td>{t('protocolsCep.codeNOMOCK')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">FREF</code></td>
            <td>{t('protocolsCep.codeFREF')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">DIFF</code></td>
            <td>{t('protocolsCep.codeDIFF')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">ABBREV</code></td>
            <td>{t('protocolsCep.codeABBREV')}</td>
          </tr>
          <tr>
            <td><code class="cep-code text-accent">SYMBOLS</code></td>
            <td>{t('protocolsCep.codeSYMBOLS')}</td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</section>

<div class="section-divider content-grid"></div>

<!-- ═══════ COMMUNICATION RULES ═══════ -->
<section class="section-y">
  <div class="content-grid">
    <p class="fluid-label text-muted animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      {t('protocolsCep.rulesLabel')}
    </p>
    <h2 class="font-display fluid-h2 font-bold text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.25rem, 1rem + 0.5vw, 2rem);">
      {t('protocolsCep.rulesTitle')}
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(3rem, 2rem + 3vw, 6rem); max-width: 48rem;">
      {t('protocolsCep.rulesDesc')}
    </p>

    <div class="cep-rules-list animate-entrance stagger-3">
      <div class="cep-rule-item">
        <span class="cep-rule-num font-display text-accent">1</span>
        <div>
          <p class="fluid-body text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.rule1Title')}</p>
          <p class="fluid-sm text-muted">{t('protocolsCep.rule1Desc')}</p>
        </div>
      </div>
      <div class="cep-rule-item">
        <span class="cep-rule-num font-display text-accent">2</span>
        <div>
          <p class="fluid-body text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.rule2Title')}</p>
          <p class="fluid-sm text-muted" set:html={t('protocolsCep.rule2Desc')} />
        </div>
      </div>
      <div class="cep-rule-item">
        <span class="cep-rule-num font-display text-accent">3</span>
        <div>
          <p class="fluid-body text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.rule3Title')}</p>
          <p class="fluid-sm text-muted" set:html={t('protocolsCep.rule3Desc')} />
        </div>
      </div>
      <div class="cep-rule-item">
        <span class="cep-rule-num font-display text-accent">4</span>
        <div>
          <p class="fluid-body text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.rule4Title')}</p>
          <p class="fluid-sm text-muted">{t('protocolsCep.rule4Desc')}</p>
        </div>
      </div>
      <div class="cep-rule-item">
        <span class="cep-rule-num font-display text-accent">5</span>
        <div>
          <p class="fluid-body text-text-bright font-medium" style="margin-bottom: 0.25rem;">{t('protocolsCep.rule5Title')}</p>
          <p class="fluid-sm text-muted">{t('protocolsCep.rule5Desc')}</p>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="section-divider content-grid"></div>

<!-- ═══════ TERMINAL DEMO ═══════ -->
<section class="section-y">
  <div class="content-grid">
    <p class="fluid-label text-muted animate-entrance" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
      {t('protocolsCep.demoLabel')}
    </p>
    <h2 class="font-display fluid-h2 font-bold text-text-bright animate-entrance stagger-1" style="margin-bottom: clamp(1.25rem, 1rem + 0.5vw, 2rem);">
      {t('protocolsCep.demoTitle')}
    </h2>
    <p class="text-muted fluid-body animate-entrance stagger-2" style="margin-bottom: clamp(3rem, 2rem + 3vw, 5rem); max-width: 48rem;">
      {t('protocolsCep.demoDesc')}
    </p>

    <div class="animate-entrance stagger-3" style="max-width: 36rem;">
      <div class="terminal-chrome">
        <div class="terminal-header flex items-center gap-2 px-4 py-2.5">
          <span class="terminal-dot bg-[#ff5f57]"></span>
          <span class="terminal-dot bg-[#febc2e]"></span>
          <span class="terminal-dot bg-[#28c840]"></span>
          <span class="ml-3 text-xs text-muted font-mono">CEP Score</span>
        </div>
        <div class="terminal-body px-5 py-5">
          <pre class="text-[11px] sm:text-[12px] font-mono leading-[1.8] text-text whitespace-pre"><span class="text-accent font-bold">CEP Score: 87/100</span>

<span class="text-muted">Dimensions:</span>
  Cache hits      <span class="text-accent">&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2591;&#x2591;</span> 82%
  Mode diversity  <span class="text-accent">&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2591;</span> 91%
  Compression     <span class="text-accent">&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2591;&#x2591;</span> 85%
  Action-first    <span class="text-accent">&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2588;&#x2591;</span> 90%

<span class="text-muted">Task complexity:</span> <span class="text-accent-2">Standard</span>
<span class="text-muted">Quality score:</span>  <span class="text-accent">&gt;95% preservation</span>

<span class="text-muted">Instruction: BRIEF &mdash; Summarize approach</span>
<span class="text-muted">in 1&ndash;2 lines, then act.</span></pre>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="section-divider content-grid"></div>

<!-- ═══════ CTA ═══════ -->
<section class="section-y">
  <div class="content-grid">
    <div class="animate-entrance">
      <h2 class="font-display fluid-h2 font-bold text-text-bright" style="margin-bottom: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);">
        {t('protocolsCep.ctaTitle')}
      </h2>
      <p class="text-muted fluid-body" style="margin-bottom: clamp(2rem, 1.5rem + 1.5vw, 3.5rem); max-width: 44rem;">
        {t('protocolsCep.ctaDesc')}
      </p>

      <div class="flex flex-wrap items-center" style="gap: clamp(0.75rem, 0.5rem + 0.5vw, 1.25rem);">
        <a href={lp('/docs/getting-started')} class="btn-primary inline-flex items-center gap-2 rounded-lg" style="padding: clamp(0.75rem, 0.55rem + 0.4vw, 1rem) clamp(1.5rem, 1rem + 0.75vw, 2.5rem); font-size: clamp(0.8125rem, 0.78rem + 0.15vw, 0.9375rem);">
          {t('protocolsCep.getStarted')}
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
        </a>
        <a href={lp('/protocols')} class="btn-outline inline-flex items-center gap-2 rounded-lg" style="padding: clamp(0.75rem, 0.55rem + 0.4vw, 1rem) clamp(1.5rem, 1rem + 0.75vw, 2.5rem); font-size: clamp(0.8125rem, 0.78rem + 0.15vw, 0.9375rem);">
          {t('protocolsCep.allProtocols')}
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  /* ── How Cards ──────────────────────────────────────── */
  .cep-how-card {
    padding: clamp(1.25rem, 1rem + 0.5vw, 1.75rem);
    border: 1px solid var(--color-border);
    border-radius: clamp(10px, 0.8vw, 14px);
    background: var(--color-bg);
  }
  .cep-how-number {
    font-size: clamp(0.6875rem, 0.65rem + 0.1vw, 0.75rem);
    font-weight: 600;
    letter-spacing: 0.08em;
  }

  /* ── Dimension Cards ────────────────────────────────── */
  .cep-dim-card {
    padding: clamp(1.25rem, 1rem + 0.75vw, 2rem);
    border: 1px solid var(--color-border);
    border-radius: clamp(10px, 0.8vw, 14px);
    background: var(--color-bg);
  }
  .cep-dim-header {
    display: flex;
    align-items: center;
    gap: 0.625rem;
  }
  .cep-dim-icon {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .cep-dim-metric {
    margin-top: 1rem;
    font-size: clamp(0.6875rem, 0.65rem + 0.1vw, 0.75rem);
    font-weight: 500;
    letter-spacing: 0.04em;
  }

  /* ── Level Cards ────────────────────────────────────── */
  .cep-level-card {
    padding: clamp(1.25rem, 1rem + 0.75vw, 2rem);
    border: 1px solid var(--color-border);
    border-radius: clamp(10px, 0.8vw, 14px);
    background: var(--color-bg);
  }
  .cep-level-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    display: inline-block;
  }
  .cep-level-instruction {
    font-size: clamp(0.6875rem, 0.65rem + 0.1vw, 0.8125rem);
    padding: 0.5rem 0.75rem;
    border-radius: 6px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
  }

  /* ── Instruction Table ──────────────────────────────── */
  .cep-table-wrap {
    border: 1px solid var(--color-border);
    border-radius: clamp(10px, 0.8vw, 14px);
    overflow: hidden;
  }
  .cep-table {
    width: 100%;
    border-collapse: collapse;
    font-size: clamp(0.75rem, 0.72rem + 0.1vw, 0.875rem);
  }
  .cep-table thead {
    background: var(--color-surface);
  }
  .cep-table th {
    padding: 0.75rem 1.25rem;
    text-align: start;
    font-weight: 600;
    color: var(--color-text-bright);
    border-bottom: 1px solid var(--color-border);
    font-size: clamp(0.6875rem, 0.65rem + 0.1vw, 0.8125rem);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .cep-table td {
    padding: 0.625rem 1.25rem;
    color: var(--color-muted);
    border-bottom: 1px solid var(--color-border);
  }
  .cep-table tr:last-child td {
    border-bottom: none;
  }
  .cep-table tr:hover td {
    background: var(--color-surface);
  }
  .cep-code {
    font-family: var(--font-mono);
    font-size: clamp(0.6875rem, 0.65rem + 0.1vw, 0.8125rem);
    font-weight: 600;
    letter-spacing: 0.04em;
    white-space: nowrap;
  }

  /* ── Communication Rules ────────────────────────────── */
  .cep-rules-list {
    display: flex;
    flex-direction: column;
    gap: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);
    max-width: 48rem;
  }
  .cep-rule-item {
    display: flex;
    gap: clamp(1rem, 0.75rem + 0.5vw, 1.5rem);
    align-items: flex-start;
  }
  .cep-rule-num {
    font-size: clamp(1.25rem, 1rem + 0.5vw, 1.75rem);
    font-weight: 700;
    line-height: 1;
    flex-shrink: 0;
    width: 2rem;
    padding-top: 0.125rem;
  }
</style>

</BaseLayout>
